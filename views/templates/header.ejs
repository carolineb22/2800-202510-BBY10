<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no"/>
    <title><%=title%></title>
    <% css.forEach((sheet) => {%>
            <link rel="stylesheet" href=<%=sheet%>>
    <% }); %>
    <% if(locals.preconnect) preconnect.forEach((p) => { %>
        <link rel="preconnect" href=<%=p%> crossorigin>
    <% }) %>
</head>
<body>